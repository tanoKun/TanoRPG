package com.github.tanokun.tanorpg.util.particle;

public enum PropertyType {
    DIRECTIONAL,
    COLORABLE,
    REQUIRES_BLOCK,
    REQUIRES_ITEM,
    REQUIRES_WATER,
    RESIZEABLE;

    private PropertyType() {
    }
}
